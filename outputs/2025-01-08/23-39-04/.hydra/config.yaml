data:
  train_dir: data/processed/train
  val_dir: data/processed/val
  test_dir: data/processed/test
  use_mask_type: fuse
  num_workers: 2
model:
  num_classes: 11
  pretrained: true
training:
  epochs: 10
  batch_size: 8
  learning_rate: 0.0001
  weight_decay: 0.0001
  image_size: 256
wandb:
  project_name: football_segmentation
profiling:
  enable: false
  sort_key: time
misc:
  save_path: models/football_segmentation_model.pth
